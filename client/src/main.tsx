import React from 'react'
import ReactDOM from 'react-dom/client'
import { Analizyer } from './Analizyer'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
    <Analizyer />
)
